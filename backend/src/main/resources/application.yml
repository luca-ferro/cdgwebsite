server:
  port: 9091
spring:
  application:
    name: rifacdg
  datasource:
    url: jdbc:mysql://localhost:3306/rifacdg
    username: luiz
    password: unifal-mg
    driver-class-name: com.mysql.cj.jdbc.Driver

  flyway:
    locations: classpath:db.migration
    baseline-on-migrate: true

logging:
  level:
    org:
      springframework:
        security: TRACE
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
